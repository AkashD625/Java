package hash_map.hash_set;
import java.util.*;

public class file {
      public static void main(String[] args) {
            //Hashset is the key part of the HashMap.
            // sysntax
            //          HashSet<Integer> hs = new HashSet<>();
            HashSet<Integer> hs = new HashSet<>();
            // hs.add(10);
            // hs.add(20);
            // hs.add(30);
            // hs.remove(20);

            // hs.contains(30);

            //-----------------------------------------------------------------------------------------------------------------
            //problem -1 find the number of unique element (repeatung element is talken  once)
            int[] arr = {1,2,3,6,1,4,5,6,8};
          
            // for(int i=0;i<arr.length;i++){
            //       hs.add(arr[i]);
            //       // same value are not added in hashset
                  
            // }
            // System.out.println(hs.size());


            // problem 2 - Given arr and the k value find arr[i]+arr[j]==k using hashset
            int k=5;
            for(int i=0;i<arr.length;i++){
                  if(hs.)
            }
      }
      
}
